# Project

A starter Node.js project scaffolded with **Express**, **CORS**, **dotenv**, **Mongoose**, **Nodemon**, and **Morgan**.  
It includes a basic folder structure and a sample `userRoute` for RESTful APIs.

---

## 🚀 Features
- Express server with JSON middleware
- CORS enabled for cross‑origin requests
- Environment variables via dotenv
- Nodemon for hot reload during development
- Morgan for request logging
- Modular routes (`userRoute.js`)
- Ready for MongoDB integration with Mongoose

---

## 📂 Project Structure

project/ 
├── src/ 
│ └── server.js 
├── routes/ 
│ └── userRoute.js 
├── controllers/ 
├── config/ 
├── .env 
├── .gitignore 
├── package.json 
└── node_modules/